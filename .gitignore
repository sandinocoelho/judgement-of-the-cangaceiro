# Built application files
*.apk

# Files for the Dalvik VM
*.dex

# Java class files
*.class

# Gradle build files
.gradle/
build/
release/

# CI Android SDK cache (see .gitlab-ci.yml)
.android-sdk/

# Local configuration file (sdk path, etc)
local.properties

# Release signing (CANGA-28) — keystore and its credentials must NEVER be committed
keystore.properties
*.jks
*.keystore

#intellij project files
*.iml
*.idea
