# Template for release signing config (CANGA-28).
# Copy to keystore.properties (gitignored) and fill in from the password manager.
# NEVER commit the real file, the keystore, or any password.
storeFile=C:/path/to/judgement-release.jks
storePassword=
keyAlias=judgement
keyPassword=
